---
    
---

<section class="relative min-h-screen w-full overflow-hidden">
  <!-- Video de fondo -->
  <div class="absolute inset-0 z-0">
    <video 
      src="/videos/c0d18810-c140-4d32-82e6-4ce0a1f42444.MP4" 
      autoplay 
      loop 
      muted 
      playsinline
      class="w-full h-full object-cover"
    ></video>
    <!-- Overlay oscuro agresivo -->
    <div class="absolute inset-0 bg-gradient-to-br from-war-primary/95 via-war-cool/90 to-war-primary/96"></div>
    <!-- Patrón de textura militar -->
    <div class="absolute inset-0 opacity-15 bg-gradient-to-r from-transparent via-war-medium/20 to-transparent"></div>
  </div>

  <!-- Contenido principal -->
  <div class="relative z-10 flex items-center justify-center min-h-screen px-4 sm:px-6 lg:px-8 py-20">
    <div class="text-center max-w-7xl mx-auto w-full">
      
      <!-- Badge superior -->
      <div class="inline-flex items-center px-3 sm:px-4 py-2 mb-6 sm:mb-8 text-xs font-semibold tracking-wider uppercase bg-black/60 border border-war-medium rounded-full backdrop-blur-sm text-war-light">
        <span class="w-1.5 h-1.5 sm:w-2 sm:h-2 bg-war-warm rounded-full mr-2 animate-pulse"></span>
        <span class="text-xs sm:text-sm">Live Combat Experience</span>
      </div>

      <!-- Título principal -->
      <h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl font-black mb-6 sm:mb-8 tracking-tight leading-tight">
        <span class="block bg-clip-text text-transparent mb-2 sm:mb-4 bg-gradient-to-r from-war-white via-war-light to-war-medium font-mono font-extrabold">
          VIVE EL COMBATE REAL
        </span>
      </h1>

      <!-- Subtítulo principal -->
      <p class="text-base sm:text-lg md:text-xl lg:text-2xl mb-4 sm:mb-6 max-w-4xl mx-auto leading-relaxed font-medium text-war-white font-mono uppercase tracking-normal px-4">
        Helicópteros, tanques, armamento auténtico y misiones diseñadas para ti en el corazón de Polonia.
      </p>

      <!-- Subtítulo secundario -->
      <p class="text-sm sm:text-base md:text-lg mb-8 sm:mb-12 max-w-3xl mx-auto leading-relaxed font-light text-war-light font-mono uppercase tracking-widest px-4">
        Siente la adrenalina de una experiencia militar personalizada, guiada por instructores cualificados y ambientada en multitud de escenarios.
      </p>

      <!-- Botón de acción principal -->
      <div class="mb-12 sm:mb-16">
        <button class="group relative px-6 sm:px-8 lg:px-10 py-3 sm:py-4 lg:py-5 bg-war-light text-black border-2 border-war-light font-bold text-base sm:text-lg lg:text-xl uppercase tracking-wider transition-all duration-300 transform hover:scale-105 hover:bg-transparent hover:text-war-light">
          <span class="relative z-10">🔘 Diseña tu misión</span>
        </button>
      </div>

      <!-- Indicadores de características -->
      <div class="flex flex-col sm:flex-row flex-wrap justify-center items-center gap-4 sm:gap-6 lg:gap-8 text-xs sm:text-sm px-4">
        <div class="flex items-center text-war-light">
          <div class="w-2 h-2 sm:w-3 sm:h-3 bg-war-warm rounded-full mr-2 animate-pulse"></div>
          <span class="uppercase tracking-wider">Armamento Real</span>
        </div>
        <div class="flex items-center text-war-medium">
          <div class="w-2 h-2 sm:w-3 sm:h-3 bg-war-gold rounded-full mr-2"></div>
          <span class="uppercase tracking-wider">Instructores Cualificados</span>
        </div>
        <div class="flex items-center text-war-light">
          <div class="w-2 h-2 sm:w-3 sm:h-3 bg-war-blue rounded-full mr-2 animate-pulse"></div>
          <span class="uppercase tracking-wider">Experiencia Personalizada</span>
        </div>
      </div>
    </div>
  </div>

  <!-- Elementos decorativos - Solo en desktop -->
  <div class="absolute top-20 sm:top-25 left-4 sm:left-10 opacity-60 sm:opacity-80 text-war-light hidden sm:block">
    <div class="text-xs font-mono uppercase tracking-widest">
      <div>Lat: 51.7592°N</div>
      <div>Lng: 19.4560°E</div>
      <div class="mt-2">Location: <span class="text-war-white">POLONIA</span></div>
    </div>
  </div>

  <div class="absolute top-20 sm:top-25 right-4 sm:right-10 opacity-60 sm:opacity-80 text-war-light hidden sm:block">
    <div class="text-xs font-mono uppercase tracking-widest text-right">
      <div>Status: <span class="text-war-warm">OPERATIONAL</span></div>
      <div>Training: <span class="text-war-gold">ACTIVE</span></div>
      <div class="mt-2">Time: <span id="military-time" class="text-war-white"></span></div>
    </div>
  </div>

  <!-- Líneas decorativas -->
  <div class="absolute bottom-0 left-0 w-full h-1 bg-gradient-to-r from-transparent via-war-light to-transparent"></div>
  <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-transparent via-war-medium to-transparent"></div>

  <!-- Elementos geométricos adicionales -->
  <div class="absolute bottom-10 left-10 opacity-30">
    <div class="w-20 h-20 border-2 border-war-light rotate-45"></div>
  </div>
  
  <div class="absolute bottom-10 right-10 opacity-30">
    <div class="w-16 h-16 border-2 border-war-medium"></div>
  </div>

<!-- Grid sutil de fondo -->
  <div class="absolute inset-0 opacity-5" style="background-image: linear-gradient(var(--color-war-light) 1px, transparent 1px), linear-gradient(90deg, var(--color-war-light) 1px, transparent 1px); background-size: 50px 50px;"></div>
</section>

<script>
  // Actualizar reloj militar
  function updateMilitaryTime() {
    const now = new Date();
    const militaryTime = now.toLocaleTimeString('en-US', { 
      hour12: false, 
      hour: '2-digit', 
      minute: '2-digit', 
      second: '2-digit' 
    });
    const timeElement = document.getElementById('military-time');
    if (timeElement) {
      timeElement.textContent = militaryTime;
    }
  }
  
  // Solo ejecutar si el elemento existe (desktop)
  if (document.getElementById('military-time')) {
    updateMilitaryTime();
    setInterval(updateMilitaryTime, 1000);
  }
</script>

<style>
  /* Animaciones personalizadas */
  
  /* Efecto de escaneo con color gris dominante - responsivo */
  section::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--color-war-light), transparent);
    animation: scan 3s linear infinite;
    z-index: 20;
  }

  
  @keyframes scan {
    0% { left: -100%; }
    100% { left: 100%; }
  }

  /* Efecto de partículas sutiles con grises - Más sutil en móviles */
  section::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
      radial-gradient(circle at 20% 80%, rgb(from var(--color-war-light) r g b / 0.03) 0%, transparent 50%),
      radial-gradient(circle at 80% 20%, rgb(from var(--color-war-medium) r g b / 0.03) 0%, transparent 50%);
    pointer-events: none;
    z-index: 5;
  }

  @media (min-width: 640px) {
    section::after {
      background-image: 
        radial-gradient(circle at 20% 80%, rgb(from var(--color-war-light) r g b / 0.05) 0%, transparent 50%),
        radial-gradient(circle at 80% 20%, rgb(from var(--color-war-medium) r g b / 0.05) 0%, transparent 50%),
        radial-gradient(circle at 40% 40%, rgb(from var(--color-war-white) r g b / 0.03) 0%, transparent 50%);
    }
  }

  /* Efecto de brillo sutil en elementos blancos */
  @keyframes subtle-glow {
    0%, 100% { text-shadow: 0 0 3px rgb(from var(--color-war-light) r g b / 0.3); }
    50% { text-shadow: 0 0 8px rgb(from var(--color-war-white) r g b / 0.4); }
  }

  @media (min-width: 640px) {
    @keyframes subtle-glow {
      0%, 100% { text-shadow: 0 0 5px rgb(from var(--color-war-light) r g b / 0.3); }
      50% { text-shadow: 0 0 15px rgb(from var(--color-war-white) r g b / 0.4); }
    }
  }
  
  h1 span:first-child {
    animation: subtle-glow 3s ease-in-out infinite;
  }

  /* Asegurar que el video sea responsive */
  video {
    object-position: center;
  }

  @media (max-width: 640px) {
    video {
      object-position: center 30%;
    }
  }
</style>
  